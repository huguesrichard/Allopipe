conda {
    enabled       = true
    cacheDir      = "$projectDir/conda-cache-nextflow"
    createTimeout = "1 h"
}

process {
    withLabel:allopipe { conda = "$projectDir/recipes/conda/allopipe.yml" }
}
